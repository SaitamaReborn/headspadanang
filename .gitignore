.places.lock
# Photographs are fetched from Google and copied into docs/ at build time.
# Committing the source copy as well doubles every commit, and the bi-monthly
# refresh replaces the whole set — history would balloon for no benefit.
assets/places/
.places-keep/
